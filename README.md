# Simple TCP Stream Wrapper

A simple tcp stream wrapper built in rust to send messages to and from both clients and server

Needs to be implemented:
Disconnecting/Forgetting clients
Proper error handling